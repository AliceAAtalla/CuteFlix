import styled from 'styled-components';
import Logo from '../../assets/img/CuteFlix.png';

const LogoCute = styled.img.attrs({ src: Logo, alt: 'CuteFlix Logo' })`
  height: 40rem;
`;

export default LogoCute;
