import styled from 'styled-components';

const DescriptionCute = styled.p`
  font-size: 18rem;
  color: var(--color-blue-dark);
  font-weight: 300;

  @media (max-width: 1300px) {
    max-width: 460rem;
  }
`;

export default DescriptionCute;
